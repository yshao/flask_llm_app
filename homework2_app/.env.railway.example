# ============================================================================
# RAILWAY DEPLOYMENT ENVIRONMENT VARIABLES
# ============================================================================
# Copy these variables to your Railway project settings:
# 1. Go to your Railway project
# 2. Select the service
# 3. Click "Variables" tab
# 4. Add each variable below
#
# Note: When adding PostgreSQL service in Railway, use reference syntax:
#    DATABASE_HOST=${{Postgres.HOSTNAME}}
#    DATABASE_NAME=${{Postgres.DATABASE}}
#    etc.
# ============================================================================

# -----------------------------------------------------------------------------
# GOOGLE GEMINI API (REQUIRED)
# -----------------------------------------------------------------------------
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash
GEMINI_MAX_TOKENS=4000
GEMINI_TEMPERATURE=0.7
GEMINI_MAX_CONVERSATION_HISTORY=1

# -----------------------------------------------------------------------------
# GROQ API (REQUIRED for Chatbot)
# -----------------------------------------------------------------------------
# Get your API key from: https://console.groq.com
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile
GROQ_MAX_TOKENS=4000
GROQ_TEMPERATURE=0.7
GROQ_MAX_CONVERSATION_HISTORY=1

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# After adding PostgreSQL service in Railway, use these references:
#
# DATABASE_HOST=${{Postgres.HOSTNAME}}
# DATABASE_PORT=${{Postgres.PORT}}
# DATABASE_NAME=${{Postgres.DATABASE}}
# DATABASE_USER=${{Postgres.USERNAME}}
# DATABASE_PASSWORD=${{Postgres.PASSWORD}}
#
# For manual database configuration:
# DATABASE_HOST=your_database_host
# DATABASE_PORT=5432
# DATABASE_NAME=homework_db
# DATABASE_USER=postgres
# DATABASE_PASSWORD=your_database_password

# PostgreSQL version for Docker (if using Docker postgres)
POSTGRES_VERSION=pgvector/pgvector:pg16

# -----------------------------------------------------------------------------
# FLASK CONFIGURATION
# -----------------------------------------------------------------------------
# Railway automatically sets PORT, no need to set it manually
FLASK_ENV=production
FLASK_DEBUG=false
SECRET_KEY=generate_a_secure_random_secret_key_here
SEND_FILE_MAX_AGE_DEFAULT=0

# -----------------------------------------------------------------------------
# RATE LIMITING
# -----------------------------------------------------------------------------
RATE_LIMIT_QUOTA=1000
RATE_LIMIT_INTERVAL=1

# -----------------------------------------------------------------------------
# SYSTEM CONFIGURATION
# -----------------------------------------------------------------------------
TIMEZONE=America/New_York
ENVIRONMENT=production

# -----------------------------------------------------------------------------
# ENCRYPTION CONFIGURATION
# -----------------------------------------------------------------------------
# Generate strong random values for production
# Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
ENCRYPTION_ONEWAY_SALT=generate_a_random_salt_string_here
ENCRYPTION_ONEWAY_N=32
ENCRYPTION_ONEWAY_R=9
ENCRYPTION_ONEWAY_P=1
ENCRYPTION_REVERSIBLE_KEY=generate_a_secure_encryption_key_here

# ============================================================================
# QUICK START GUIDE
# ============================================================================
#
# 1. Create Railway Account:
#    Go to https://railway.app and sign up
#
# 2. Create New Project:
#    - Click "New Project" → "Deploy from GitHub repo"
#    - Select your flask-llm-app repository
#
# 3. Add PostgreSQL Service:
#    - In your project, click "New Service" → "Database" → "Add PostgreSQL"
#    - Railway will provide connection details
#
# 4. Configure Environment Variables:
#    - Go to your Flask service → "Variables" tab
#    - Add all variables above
#    - For database, use Railway references: ${{Postgres.HOSTNAME}}, etc.
#
# 5. Deploy:
#    - Railway will auto-deploy on push
#    - Or click "Deploy Now" button
#
# 6. Initialize Database (first time only):
#    - Go to "Deployments" tab
#    - Click on your deployment
#    - Click "Exec" button to open shell
#    - Run: python -c "from flask_app import create_app, db; app = create_app(); with app.app_context(): db.create_all(); print('Database initialized!')"
#
# 7. Access Your App:
#    - Go to "Domains" tab
#    - Click on your generated domain
# ============================================================================
